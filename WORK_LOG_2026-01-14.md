# 作業ログ: 2026-01-14

## 概要

Tab5「福祉用具選定」に3パターンの入力モーダルを追加し、Daily Sync後のデータ保持問題を修正しました。

---

## 1. 福祉用具選定の入力モーダル追加

### 背景
- 既存の「機器を追加」ボタンは、テーブルに直接行を追加するだけで入力フォームがなかった
- 販売パターンの追加要望があり、3パターン全てに入力モーダルを実装

### 実装内容

#### パターン選択モーダル
「機器を追加」ボタンをクリックすると、以下の3つから選択するモーダルを表示：
- 介護保険レンタル（青色）
- 自費レンタル（紫色）
- 販売（緑色）

#### 介護保険レンタル入力モーダル（青色テーマ）

| セクション | 入力項目 |
|-----------|---------|
| 商品情報 | 福祉用具の種類、メーカー、商品名（カスケードフィルタ対応）、商品コード（自動）、単位数（自動）、卸会社 |
| 管理情報 | 属性、カイポケ登録、記録者 |
| 日程情報 | 受注日、発注日、納品日、利用開始日、利用終了日 |
| その他 | 備考 |

**特徴:**
- 福祉用具マスターデータ（928件）とカスケードフィルタ連携
- 商品名選択で商品コード・単位数を自動入力

#### 自費レンタル入力モーダル（紫色テーマ）

| セクション | 入力項目 |
|-----------|---------|
| 商品情報 | 商品名、卸会社 |
| 料金情報 | 数量、単価（税抜）、税区分、税込金額（自動計算） |
| 日程情報 | 利用開始日、利用終了日 |
| その他 | 備考 |

**特徴:**
- 税込金額を自動計算（税区分に応じて10%/8%/0%）
- カイポケ登録は不要のため削除

#### 販売入力モーダル（緑色テーマ）

| セクション | 入力項目 |
|-----------|---------|
| 商品情報 | 商品名、受注日、納品日、営業担当 |
| 料金情報 | 数量、単価（税抜）、税区分、税込金額（自動計算）、送料 |
| 取引情報 | 取引内容、支払い方法、利用者自己負担割合 |
| 申請情報 | 申請、申請市町村 |
| その他 | 備考 |

**特徴:**
- 税込金額を自動計算
- 販売特有のフィールド（営業担当、取引内容、支払い方法等）を追加

### テーブル編集機能
- 各テーブルの行をクリックすると、対応する入力モーダルが開く（編集モード時）
- 削除ボタンはクリックイベントが伝播しないよう修正

### 追加した型定義（types.ts）

```typescript
// 販売用の型定義
export type TransactionType = '社内間取引' | '社内外取引';
export type UserBurdenType = '自己負担０（日常生活給付）' | '一部負担（日常生活給付）' | '１割負担（受領委任払い）' | '２割負担（受領委任払い）' | '３割負担（受領委任払い）' | '全額負担（償還払い）';
export type PaymentMethod = '口座引き落とし' | '現金集金' | '受領委任払い' | '償還払い' | '日常生活給付';

// Equipmentインターフェースに追加
salesPerson?: string;              // 営業担当
transactionType?: TransactionType; // 取引内容
userBurdenType?: UserBurdenType;   // 利用者自己負担割合
paymentMethod?: PaymentMethod;     // 支払い方法
applicationStatus?: boolean;       // 申請
applicationMunicipality?: string;  // 申請市町村
shippingCost?: number;             // 送料金額
```

---

## 2. Daily Syncデータ保持問題の修正

### 問題
Daily Sync（毎日00:00 JST）後に以下のデータが消失する可能性があった：
- 手動追加された自費レンタル用具
- 販売用具

### 原因
`importSpreadsheetData.cjs`が介護保険レンタルのみ保持し、自費レンタル・販売を保持していなかった。

### 修正内容

**修正前:**
```javascript
const existingInsuranceRentalEquipmentMap = new Map();
// 介護保険レンタルのみ保持
```

**修正後:**
```javascript
const existingInsuranceRentalEquipmentMap = new Map();
const existingSelfPayRentalEquipmentMap = new Map();
const existingSalesEquipmentMap = new Map();

// ステータス別に全て保持
if (client.selectedEquipment && client.selectedEquipment.length > 0) {
  // 介護保険レンタル
  const insuranceRentals = client.selectedEquipment.filter(e => e.status === '介護保険レンタル');
  // 自費レンタル
  const selfPayRentals = client.selectedEquipment.filter(e => e.status === '自費レンタル');
  // 販売
  const salesItems = client.selectedEquipment.filter(e => e.status === '販売');
}

// 結合時に全てを含める
const combinedEquipment = [
  ...selfPayEquipment,           // スプレッドシートからの自費
  ...existingInsuranceRentals,   // 既存の介護保険
  ...uniqueExistingSelfPay,      // 既存の自費（重複除外）
  ...existingSales               // 既存の販売
];
```

### 保持されるデータ一覧

| データ | 保持状態 |
|--------|---------|
| changeRecords（Kintone） | ✅ |
| insuranceNumber（被保険者番号） | ✅ |
| kaipokeRegistrationStatus | ✅ |
| selectedEquipment（介護保険レンタル） | ✅ |
| selectedEquipment（自費レンタル） | ✅ **新規** |
| selectedEquipment（販売） | ✅ **新規** |

---

## 3. 自費レンタルからカイポケ登録を削除

### 変更内容
- 自費レンタルテーブルから「カイポケ」列を削除
- 自費レンタル入力モーダルから「カイポケ登録」フィールドを削除

### 理由
自費レンタルはカイポケへの登録が不要なため。

---

## 本日のコミット一覧

| コミット | 内容 |
|---------|------|
| `6defb32` | feat: Add equipment input modals for all types + preserve data on Daily Sync |

---

## 現在のデータ統計

| 項目 | 件数 |
|------|------|
| 総クライアント数 | 8,474件 |
| 介護保険レンタル | 1,437件 |
| 自費レンタル | 119件（107クライアント） |
| 販売 | 0件（新機能） |

---

## 更新されたファイル

| ファイル | 変更内容 |
|---------|---------|
| `components/ClientDetail.tsx` | 3パターンの入力モーダル追加、テーブル編集機能 |
| `types.ts` | 販売用の型定義追加 |
| `importSpreadsheetData.cjs` | 自費レンタル・販売データの保持対応 |
| `SYNC_SETUP.md` | データ保持に関するドキュメント更新 |

---

## 4. 販売データ用スプレッドシート作成

### 作成内容
既存の売上スプレッドシートに「販売データ」シートを追加。

**スプレッドシート情報:**
- URL: https://docs.google.com/spreadsheets/d/1zeU-ui7jtc1djMn4virPyHJeb5zIL7c-Sbr1D4oH6nQ
- シート名: 販売データ

### ヘッダー構成

| 列 | 項目 | 備考 |
|---|------|------|
| A | あおぞらID | 必須 |
| B | 利用者名 | 参照用 |
| C | 商品名 | 必須 |
| D | 受注日 | YYYY-MM-DD |
| E | 納品日 | YYYY-MM-DD |
| F | 営業担当 | |
| G | 数量 | |
| H | 単価（税抜） | |
| I | 税区分 | プルダウン（非課税/10％/軽8％/税込） |
| J | 税込金額 | |
| K | 送料 | |
| L | 取引内容 | プルダウン（社内間取引/社内外取引） |
| M | 支払い方法 | プルダウン |
| N | 利用者自己負担割合 | プルダウン |
| O | 申請 | チェックボックス |
| P | 申請市町村 | |
| Q | 備考 | |

### インポートスクリプト
`importSalesData.cjs` を作成。

```bash
# 販売データインポート手順
node importSalesData.cjs
npm run build
firebase deploy --only hosting
```

---

## スプレッドシート一覧（参照用）

| 用途 | スプレッドシートID | シート名 |
|------|-------------------|---------|
| 自費レンタル | 1v_TEkErlpYJRKJADX2AcDzIE2mJBuiwoymVi1quAVDs | シート1, 12月実績 |
| 販売データ | 1zeU-ui7jtc1djMn4virPyHJeb5zIL7c-Sbr1D4oH6nQ | 販売データ |
| 介護保険レンタル | 1TduZae5tt7ZMsop6OlDK3Q6r27DJuqguYe4CvtcPJDs | サービスチェックシート |

---

## デプロイ

- **Firebase Hosting:** https://welfare-assist-pro.web.app
- デプロイ日時: 2026-01-14
