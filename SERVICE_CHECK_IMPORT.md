# サービスチェックシート連携機能実装記録

## 概要

サービスチェックシート（令和7年12月実績）から介護保険レンタルデータをインポートし、Tab5「福祉用具選定」に一覧表示する機能を実装しました。

実装日: 2026-01-12

## データソース

- **スプレッドシート**: サービスチェックシート
- **スプレッドシートID**: 1TduZae5tt7ZMsop6OlDK3Q6r27DJuqguYe4CvtcPJDs
- **シート名**: サービスチェックシート
- **データ期間**: 令和7年12月
- **総レコード数**: 1,418件
- **利用者数**: 360人

## データ統計

### 基本統計
- 総レコード数: 1,418件
- ユニーク利用者数: 360人
- マッチング成功: 357人 (99.2%)
- マッチング失敗: 3人
- 追加された介護保険レンタル用具: 1,448件

### 品目内訳（トップ5）
1. 特殊寝台付属品: 344件
2. 特殊寝台: 289件
3. 車いす: 212件
4. 手すり: 175件
5. 歩行器: 111件

### メーカー内訳（トップ3）
1. パラマウントベッド: 466件
2. プラッツ: 207件
3. DIPPER北明: 124件

## 実装内容

### 1. データ構造の拡張

#### types.ts
```typescript
export interface Client {
  id: string;
  aozoraId: string;
  insuranceNumber?: string; // 被保険者番号 ← 追加
  office: OfficeLocation;
  // ... その他のフィールド
}
```

**追加フィールド:**
- `insuranceNumber`: 被保険者番号（オプショナル）
  - サービスチェックシートとのマッチングに使用
  - クライアントの一意識別子として機能

### 2. データインポートスクリプト

#### importServiceCheckSheet.cjs
サービスチェックシートからデータをインポートするスクリプトを作成。

**機能:**
- Google Sheets APIで「サービスチェックシート」を読み込み
- 被保険者番号または氏名でclients.jsonとマッチング
- `selectedEquipment`配列に介護保険レンタル用具を追加
- `insuranceNumber`フィールドを設定

**実行方法:**
```bash
node importServiceCheckSheet.cjs
```

**データマッピング:**
| サービスチェックシート | Equipment フィールド |
|---------------------|-------------------|
| A列: 被保険者番号 | (insuranceNumberに保存) |
| B列: 利用者名 | (マッチングキー) |
| H列: 商品コード | taisCode |
| I列: 品目 | category |
| J列: メーカー | manufacturer |
| K列: 卸業者 | wholesaler |
| L列: 商品名 | name |
| M列: 単位数 | units |

**生成されるフィールド:**
- `id`: UUID v4で自動生成
- `status`: "介護保険レンタル"（固定値）
- `office`: クライアントのofficeフィールドから継承
- `kaipokeStatus`: "未登録"（デフォルト値）

### 3. Firestoreマージロジックの修正

#### 問題
`firestoreService.ts`のマージロジックで、Firestoreに空配列 `[]` がある場合、それがtruthyと判定されてbase dataを上書きしていた。

#### 修正内容（src/services/firestoreService.ts:140-143）
```typescript
// 修正前
selectedEquipment: edits.selectedEquipment || baseClient.selectedEquipment || [],

// 修正後
selectedEquipment: (edits.selectedEquipment?.length ? edits.selectedEquipment : baseClient.selectedEquipment) || [],
```

**修正の意図:**
- 空配列の場合はbase dataを優先
- データがある場合のみFirestoreのデータを使用
- 同様の修正を`meetings`, `changeRecords`, `plannedEquipment`にも適用

### 4. Tab5「福祉用具選定」のUI改善

#### 変更前
- 各用具が大きなカード形式で表示
- 詳細フィールドが展開表示
- スクロールが長くなる

#### 変更後
- テーブル形式の一覧表示
- 介護保険レンタルと自費レンタルをグループ化
- コンパクトで見やすい表示

#### 介護保険レンタルテーブル（青色ヘッダー）

**列構成:**
1. 商品名
2. メーカー
3. 卸会社
4. 種類（福祉用具の種類）
5. 単位数
6. 商品コード
7. 利用開始日
8. 利用終了日
9. カイポケ（バッジ表示）
10. 操作（編集モード時のみ：削除ボタン）

**特徴:**
- 青系グラデーションヘッダー
- 件数バッジ表示
- ホバー時に行背景が青色に変化
- カイポケ登録済みは緑、未登録は灰色バッジ

#### 自費レンタルテーブル（紫色ヘッダー）

**列構成:**
1. 商品名
2. 卸会社
3. 単価（¥表示）
4. 数量
5. 税込金額（太字、¥表示）
6. 利用開始日
7. 利用終了日
8. カイポケ（バッジ表示）
9. 操作（編集モード時のみ：削除ボタン）

**特徴:**
- 紫系グラデーションヘッダー
- 件数バッジ表示
- ホバー時に行背景が紫色に変化
- 金額フィールドは自動的に3桁区切り表示

### 5. 卸会社列の追加

**目的:**
- 卸会社の請求書との突合を容易にする
- 卸会社ごとの集計が可能になる

**対応:**
- 介護保険レンタルテーブルに卸会社列を追加（メーカーの次）
- 自費レンタルテーブルに卸会社列を追加（商品名の次）

## ファイル変更一覧

### 新規作成
- `importServiceCheckSheet.cjs` - サービスチェックシートインポートスクリプト
- `matchServiceCheckData.cjs` - マッチング確認スクリプト
- `analyzeServiceCheckSheet.cjs` - データ分析スクリプト
- `inspectNewSpreadsheet.cjs` - スプレッドシート構造確認スクリプト
- `checkFirestoreSelectedEquipment.cjs` - Firestore確認スクリプト
- `fix-insurance-table.cjs` - 介護保険テーブル形式変換スクリプト
- `fix-selfpay-table.cjs` - 自費レンタルテーブル形式変換スクリプト
- `SERVICE_CHECK_IMPORT.md` - 本ドキュメント

### 変更
- `types.ts` - insuranceNumberフィールド追加
- `src/services/firestoreService.ts` - マージロジック修正
- `components/ClientDetail.tsx` - Tab5のテーブル表示実装
- `clients.json` - 介護保険レンタルデータ追加、insuranceNumber追加
- `public/assets/clients.json` - clients.jsonをコピー

## データフロー

```
┌─────────────────────────────────────────┐
│  Google Sheets                          │
│  サービスチェックシート                  │
│  - 1,418件のレコード                     │
│  - 360人の利用者                         │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  importServiceCheckSheet.cjs            │
│  - 被保険者番号/氏名でマッチング          │
│  - 357人マッチング成功（99.2%）           │
│  - 1,448件の用具データを追加              │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  clients.json (ルート)                   │
│  - selectedEquipment配列に追加           │
│  - insuranceNumber設定                  │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  public/assets/clients.json             │
│  (ビルド時にコピー)                      │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  App.tsx                                │
│  - /assets/clients.json 読み込み        │
│  - Firestoreとマージ                    │
└────────────────┬────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────┐
│  ClientDetail.tsx - Tab5                │
│  - 介護保険レンタルテーブル表示          │
│  - 自費レンタルテーブル表示              │
└─────────────────────────────────────────┘
```

## ビルド・デプロイ手順

### 1. データインポート
```bash
# サービスチェックシートからインポート
node importServiceCheckSheet.cjs

# ルートからpublic/assetsにコピー
cp clients.json public/assets/clients.json
```

### 2. ビルド
```bash
npm run build
# copy-clients.cjsが自動実行されdist/assets/clients.jsonにコピー
```

### 3. デプロイ
```bash
firebase deploy --only hosting
```

## 今後の課題・改善案

### データ同期
- [x] ~~サービスチェックシートの定期インポート~~ ✅ 週次手動運用に決定（2026-01-13）
- [ ] 差分更新機能（既存データの上書き/追加の判定）
- [ ] 削除された用具の検出と処理

### UI改善
- [ ] テーブルのソート機能（卸会社、メーカー、種類など）
- [ ] テーブルのフィルタ機能（カイポケ登録状況など）
- [ ] CSV/Excelエクスポート機能（請求書突合用）
- [ ] 卸会社ごとの集計表示

### データ検証
- [ ] 商品コードの一貫性チェック
- [ ] 単位数の妥当性検証
- [ ] 重複データの検出

### 請求書突合機能
- [ ] 卸会社別一覧表示
- [ ] 月次集計レポート
- [ ] 請求書データのアップロードと突合
- [ ] 差異検出機能

## 関連ドキュメント

- [DECEMBER_DATA_IMPORT.md](./DECEMBER_DATA_IMPORT.md) - 12月実績データ反映の記録
- [CLAUDE.md](./CLAUDE.md) - プロジェクト全体の技術ドキュメント
- [README.md](./README.md) - プロジェクト概要

## 変更履歴

| 日付 | 変更内容 | コミット |
|-----|---------|---------|
| 2026-01-12 | サービスチェックシート連携機能実装 | e989126 |
| 2026-01-12 | Tab5テーブル表示化 | e989126 |
| 2026-01-12 | 卸会社列追加 | e989126 |
| 2026-01-13 | 週次手動運用に変更（daily-syncから除外） | - |
